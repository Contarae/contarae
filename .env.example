# Valores de desarrollo sin secretos. Copia este archivo como .env localmente.
CI=false
DISABLE_ESLINT_PLUGIN=true
NETLIFY_FORM_NAME=certificacion

# Configurar solo en el entorno privado del servidor (por ejemplo, Netlify).
WOMPI_EVENTS_SECRET=
